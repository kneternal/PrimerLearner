#include <vector>

using namespace std;

vector<int> merge(vector<int> &, vector<int> &);
vector<int> merge1(vector<int>, vector<int>);

vector<int> mergesort(const vector<int> &);
vector<int> mergesort1(const vector<int> &);
